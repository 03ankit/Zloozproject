<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <ImageView
        android:id="@+id/expanded_avatar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:scaleType="centerCrop"
        android:layout_alignParentStart="true"
        android:clipToOutline="true"/>

    <ImageView
        android:id="@+id/expanded_badge"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignBottom="@id/expanded_avatar"
        android:layout_alignEnd="@id/expanded_avatar"
        android:layout_marginBottom="2dp"
        android:layout_marginEnd="2dp"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/expanded_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/expanded_avatar"
        android:layout_marginStart="12dp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:maxLines="1"
        android:ellipsize="end"/>

    <TextView
        android:id="@+id/expanded_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/expanded_avatar"
        android:layout_below="@id/expanded_title"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        android:textColor="@android:color/black"
        android:maxLines="5"
        android:ellipsize="end"/>

</RelativeLayout>
